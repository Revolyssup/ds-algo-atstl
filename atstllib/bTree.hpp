#pragma once
#include "node.hpp"
#include "utils.hpp"
namespace atstl{
    template<typename T>
    class btree{
        btreenode<T> root(0);
        public:
        void insert(T key){
            
        }

    };
}